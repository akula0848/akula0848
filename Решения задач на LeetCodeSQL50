Решения задач на Leet_CODE_SQL50
1757. Recyclable and Low Fat Products


/* Write your T-SQL query statement below */
SELECT
    pro.product_id

FROM
    Products AS pro
INNER JOIN
    Products AS pr
    ON pro.product_id = pr.product_id 
WHERE
    pro.low_fats = 'Y'
    AND pr.recyclable = 'Y'
    AND pro.low_fats = pr.recyclable
